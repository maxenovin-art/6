سیستم مدیریت کاربران و احراز هویت (FastAPI + Vercel)

این پروژه یک سیستم ساده و کاربردی برای مدیریت کاربران و احراز هویت است که با Python و FastAPI پیاده‌سازی شده و قابلیت اجرا هم به‌صورت محلی (Command Prompt / PowerShell) و هم به‌صورت آنلاین روی Vercel را دارد.

 نسخه آنلاین پروژه

لینک دپلوی‌شده روی Vercel:
👉 https://6-odun.vercel.app/


 معرفی پروژه

این سیستم امکانات پایه و ضروری مدیریت کاربران را فراهم می‌کند و برای استفاده به‌عنوان نمونه‌کار Backend یا پروژه دانشجویی مناسب است.

 امکانات پروژه

ثبت‌نام کاربر جدید

ورود (Login) کاربران

هش‌کردن رمز عبور (SHA256)

نمایش لیست کاربران

API مستندشده با Swagger

سازگار با معماری Serverless

قابل دپلوی روی Vercel

 توجه: به‌دلیل معماری Serverless در Vercel، ذخیره‌سازی کاربران به‌صورت In-Memory انجام شده و آماده اتصال به دیتابیس‌های خارجی مانند PostgreSQL یا Supabase است.

 تکنولوژی‌های استفاده‌شده

Python

FastAPI

hashlib

datetime

Vercel Serverless Functions

 پیش‌نیازها

برای اجرای محلی پروژه نیاز دارید به:

Python نسخه 3.9 یا بالاتر

pip (نصب‌شده همراه Python)

 نصب و اجرای پروژه (محلی)
 نصب وابستگی‌ها
pip install -r requirements.txt

 اجرای پروژه
uvicorn main:app --reload


 حالا پروژه روی آدرس زیر در دسترس است:

http://localhost:8000
